import React from 'react';
import { Reference, ReferenceProps } from 'react-popper';

const DropdownReference = (props: ReferenceProps) => (
  <Reference {...props} />
);

export default DropdownReference;